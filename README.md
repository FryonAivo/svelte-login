## Svelte OAuth Login

This project is practice for me implementing OAuth from different providers.
It only currently supports Google with a very simple workflow, but the scaffolding should work for any other provider as well.